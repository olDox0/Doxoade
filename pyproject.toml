[tool.doxoade]
source_dir = "doxoade"
ignore = [
    "Vers/", "node_modules", "backups", "documentacao_antiga"
]
# <<< ADICIONE ESTE BLOCO NOVO >>>
keep = [
    "pytest",   # Usado pelo 'health' via subprocess
    "coverage", # Usado pelo 'health' via subprocess
    "radon"     # Usado pelo 'health' via subprocess
]